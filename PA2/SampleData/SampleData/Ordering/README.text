
If you change the order of the rules, your program should run forever
for some queries.  This is due to the depth-first search nature of our
backward chaining algorithm.

Try queries like "(Connected B F)", "(Connected B ?x)", and
"(Connected ?y (ComputerIn MyOffice))".

